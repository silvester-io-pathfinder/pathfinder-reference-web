@using Silvester.Pathfinder.Reference.Web.Shared.Tables
@using Silvester.Pathfinder.Reference.Web.Shared.Tables.Builder
@using Silvester.Pathfinder.Reference.Web.Graphql
@using Silvester.Pathfinder.Reference.Web.Services

@inherits AbstractTable<IDeitiesPage, DeitySortInput>

@Table

@code {
    protected override void CreateModel(TableBuilder<IDeitiesPage> builder)
    {
        builder
            .AddTextColumn(e => e.Name, "Name", nameof(IDeitiesPage.Name), true)
            .AddTextColumn(e => e.Alignment.Name, "Alignment", nameof(IDeitiesPage.Alignment) + "." + nameof(IDeitiesPage_Alignment.Name), hideBelow: Breakpoint.Xs)
            .AddTextColumn(e => e.Category.Name, "Category", nameof(IDeitiesPage.Category) + "." + nameof(IDeitiesPage_Category.Name), hideBelow: Breakpoint.Xs)
            .AddTextColumn(e => e.Description, "Description", nameof(IPlanesPage.Details) + "." + nameof(ITextBlock.Text), hideBelow: Breakpoint.SmAndDown)
            .AddInspectColumn(NavigationManager, e => $"deities/{e.Id}");
    }
}