@using Silvester.Pathfinder.Reference.Web.Shared.Tables
@using Silvester.Pathfinder.Reference.Web.Shared.Tables.Builder
@using Silvester.Pathfinder.Reference.Web.Graphql
@using Silvester.Pathfinder.Reference.Web.Services

@inherits AbstractTable<ITraitsPage, TraitSortInput>

@Table

@code {
    protected override void CreateModel(TableBuilder<ITraitsPage> builder)
    {
        builder
            .AddTextColumn(e => e.Name, "Name", nameof(ITraitsPage.Name), true)
            .AddTextColumn(e => e.Details.First(e => e.Order == 0).Text, "Description", nameof(ITraitsPage.Details) + "." + nameof(ITextBlock.Text), hideBelow: Breakpoint.Xs)
            .AddInspectColumn(NavigationManager, e => $"traits/{e.Id}");
    }
}