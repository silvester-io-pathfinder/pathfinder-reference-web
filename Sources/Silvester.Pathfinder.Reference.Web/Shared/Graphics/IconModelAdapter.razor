@using Silvester.Pathfinder.Official.Web.Utilities.Extensions;

@if(Model != null)
{
    <Icon Width="@Model.Width" Height="@Model.Height"  @attributes="@Model.Splat()"/>
}

@code {
    [Parameter]
    public IconModel? Model { get; set; } = default!;
}